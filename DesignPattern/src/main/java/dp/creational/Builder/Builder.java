package dp.creational.Builder;


class Builder {
	/*
	 * if there is a large no of optional parameters
	 * it is hard to create an object with those object using constructor as we need to create multiple constructor
	 * so Builder pattern helps to create in this kind of situation
	 * 
	 * 
	 */
	
}
